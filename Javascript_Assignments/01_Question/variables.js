let name = "vicky";
let truthvalue = false;
let m = undefined;
let l = null;
console.log(`the string value is ${name} and the datatype is ${typeof name}`);
console.log(`the value of truthvalue is ${truthvalue} and the datatype is ${ typeof truthvalue}`);
console.log(`the value of m is ${m} and its datatype is ${typeof m}`);
console.log(`the value of l is ${l} and its datatype is ${typeof l}`);